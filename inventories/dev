# Inventory of hosts targeted for a developer environment

[webserver]
localhost ansible_connection=local

[broker]
localhost ansible_connection=local

[task_queue]
localhost ansible_connection=local

[workers]
localhost ansible_connection=local

# Environment-specific variables are defined in this group
[dev]
localhost ansible_connection=local
